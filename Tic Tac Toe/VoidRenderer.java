public class VoidRenderer implements Renderer {
    /**
     * do nothing. this function doesn't show anything.
     * @param board the Board class object to print.
     */
    @Override
    public void renderBoard(Board board) {
    }
}
