public interface Renderer {
     /**
      * prints the current mood of the given board in each design of the class that will be chosen.
      * @param board the Board class object to print.
      */
     void renderBoard(Board board);
}
